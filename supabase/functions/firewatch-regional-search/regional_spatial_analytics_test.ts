import {buildSpatialPlan} from "./regional_spatial.ts";
import {analyzeSpatial} from "./regional_spatial_analytics.ts";
function assert(x:unknown,m:string){if(!x)throw new Error(m)}
Deno.test("radius analytics rings sectors density",()=>{const p=buildSpatialPlan({mode:"radius",lat:49.5883,lon:34.5514,radius_m:5000});const xs=[{latitude:49.589,longitude:34.551,distance_m:100,category_keys:["fuel"]},{latitude:49.60,longitude:34.56,distance_m:1500,category_keys:["fuel"]},{latitude:49.62,longitude:34.59,distance_m:4200,category_keys:["pharmacy"]}];const a=analyzeSpatial(xs,p);assert(a.rings.length===4,"rings");assert(a.sectors.reduce((s:any,x:any)=>s+x.count,0)===3,"sector sum");assert(a.density_per_km2>0,"density");assert(a.coverage_gaps.empty_cells>0,"gaps")});
Deno.test("route analytics 5km bins and gap detection",()=>{const p=buildSpatialPlan({mode:"route",route:[[34.5,49.5],[34.8,49.5]],corridor_m:1000});const xs=[{route_along_m:1000,category_keys:["fuel"]},{route_along_m:12000,category_keys:["fuel"]}];const a=analyzeSpatial(xs,p);assert(a.route.segments.length>=4,"segments");assert(a.route.gap_count>0,"route gaps");assert(a.study_area_km2>0,"route area")});
Deno.test("polygon analytics area",()=>{const p=buildSpatialPlan({mode:"polygon",geometry:{type:"Polygon",coordinates:[[[34,49],[34.1,49],[34.1,49.1],[34,49.1],[34,49]]]}});const a=analyzeSpatial([{category_keys:["fuel"]}],p);assert(a.polygon.area_km2>50,"polygon area");assert(a.density_per_km2>0,"polygon density")});
